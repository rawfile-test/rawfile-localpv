name: CI
on:
  workflow_call:
  pull_request:
  push:
    branches:
      - develop
      - 'release/**'
  schedule:
    - cron: '0 0 * * *'
  merge_group:

jobs:
  changes:
    uses: ./.github/workflows/docs-only.yml
  
  skip:
    runs-on: ubuntu-latest
    outputs:
      skip: ${{ steps.check.outputs.skip-check }}
    steps:
      - id: merge-q
        uses: cariad-tech/merge-queue-ci-skipper@main
        with:
          secret: ${{ secrets.GITHUB_TOKEN }}
      - id: skip
        run: |
          echo "Got: ${{ steps.merge-q.outputs.skip-check }}"

  helm-ci:
    needs: changes
    if: ${{ github.event_name != 'pull_request' || needs.changes.outputs.docs-only == 'false' }}
    uses: ./.github/workflows/helm-chart.yml
  k8s-ci:
    needs: changes
    if: ${{ needs.merge-q.outputs.skip == 'false' && (github.event_name != 'pull_request' || needs.changes.outputs.docs-only == 'false') }}
    uses: ./.github/workflows/k8s.yml
  ci:
    if: ${{ success() }}
    needs:
      - helm-ci
      - k8s-ci
    runs-on: ubuntu-latest
    steps:
      - name: CI not succeeded
        run: exit 1
